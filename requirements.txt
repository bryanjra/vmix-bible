# Runtime deps for the vMix Bible engine.
# Install:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Web server + WebSocket projection
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# Natural-language parser (Spanish dictation -> RVR1960 reference)
# Uses Claude.ai OAuth token from ~/.claude/.credentials.json, no API key needed.
anthropic>=0.40,<1.0

# Agentic listener (in-process STT). The operator picks an input device +
# channel from /control; transcribed utterances feed the suggestion panel.
# sounddevice's Windows wheel bundles PortAudio so no system install needed.
faster-whisper>=1.0
sounddevice>=0.4.6
numpy>=1.24
